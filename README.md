# TrackerAPI

Fitness Tracker API built with Django.

## Installation

1. Create virtual environment: `python -m venv venv`
2. Install dependencies: `pip install -r requirements.txt`
3. Run server: `python manage.py runserver`

## Description

This is a simple API for tracking fitness activities, workouts, and nutrition.